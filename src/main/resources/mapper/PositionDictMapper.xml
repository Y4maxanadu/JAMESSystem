<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.james.system.mapper.PositionDictMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.james.system.entity.PositionDict">
        <id column="position_code" property="positionCode"/>
        <result column="position_name" property="positionName"/>
        <result column="position_desc" property="positionDesc"/>
    </resultMap>

    <!-- 根据代码查询 -->
    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT position_code, position_name, position_desc
        FROM position_dict
        WHERE position_code = #{positionCode}
    </select>

    <!-- 查询所有 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT position_code, position_name, position_desc
        FROM position_dict
        ORDER BY position_code
    </select>

    <!-- 新增 -->
    <insert id="insert">
        INSERT INTO position_dict (position_code, position_name, position_desc)
        VALUES (#{positionCode}, #{positionName}, #{positionDesc})
    </insert>

    <!-- 更新 -->
    <update id="update">
        UPDATE position_dict
        SET position_name = #{positionName},
            position_desc = #{positionDesc}
        WHERE position_code = #{positionCode}
    </update>

    <!-- 删除 -->
    <delete id="deleteByCode">
        DELETE FROM position_dict WHERE position_code = #{positionCode}
    </delete>

</mapper>
